server {
    listen 80;
    server_name _;
    root /var/www/html/public;
    index index.php;
    charset utf-8;

    # Header keamanan dasar
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;

    client_max_body_size 16M;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location = /favicon.ico { access_log off; log_not_found off; }
    location = /robots.txt  { access_log off; log_not_found off; }

    # Hanya index.php yang boleh dieksekusi sebagai PHP di root public.
    location ~ ^/index\.php(/|$) {
        fastcgi_pass app:9000;
        fastcgi_split_path_info ^(.+\.php)(/.*)$;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        fastcgi_param DOCUMENT_ROOT $realpath_root;
        internal;
    }

    # Blokir eksekusi PHP lain (mis. webshell yang terunggah ke folder publik).
    location ~ \.php$ {
        return 403;
    }

    # Blokir akses ke berkas tersembunyi & sensitif.
    location ~ /\.(?!well-known).* { deny all; }
    location ~* \.(env|bak|sql|log|sh|yaml|yml)$ { deny all; }

    error_log  /var/log/nginx/error.log;
    access_log /var/log/nginx/access.log;
}
